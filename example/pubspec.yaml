name: example
description: fluro_router_generate 示例应用，用于运行和调试
publish_to: 'none'

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  fluro_router_generate:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.11.0

flutter:
  uses-material-design: true
